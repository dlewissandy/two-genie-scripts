# Python dependencies for the figure-generation scripts.
# Install with:  pip install -r figures/requirements.txt
# Only these three third-party packages are used; everything else is stdlib.
# Verified to run on numpy 2.4.2, scipy 1.17.0, matplotlib 3.10.8 (any recent
# versions should work).
numpy
scipy
matplotlib
