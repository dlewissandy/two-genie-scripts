# Lean / Lake build artifacts
/TwoGenie/.lake/
# Note: lake-manifest.json IS committed — it pins the exact mathlib revision
# so the build is reproducible (and CI/lean-action requires it).
# Project-local toolchain (isolated install; regenerate via setup.sh)
/.elan/
# Python venv for Phase-4 plotting
/.venv/
# Generated figures / data
/TwoGenie/figures/
# Logs
*.log
